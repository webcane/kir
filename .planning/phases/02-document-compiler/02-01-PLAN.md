---
phase: 02-document-compiler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kir/core/ports/llm_port.py
  - src/kir/core/ports/parser_port.py
  - src/kir/core/domain/models/document.py
  - src/kir/core/passes/context.py
  - tests/conftest.py
  - pyproject.toml
  - tests/compiler/__init__.py
  - tests/compiler/documents/__init__.py
  - tests/llm/__init__.py
autonomous: true
requirements:
  - DOC-01
  - DOC-03
  - LLM-01
  - LLM-02
  - LLM-03

must_haves:
  truths:
    - "LLMPort Protocol has an async extract(sections, prompt) method and a model_id attribute — no old extract(text) signature remains"
    - "MarkdownParserPort.parse() return type is list[Section] — not object"
    - "Document model has a diagnostics: tuple[Diagnostic, ...] = () field that existing tests continue to pass"
    - "CompilerContext has prompt_version, llm_cache, and prompts fields with Optional defaults"
    - "uv run pytest -x -q passes (no tests collected is OK; test infra baseline is green)"
    - "pydantic-ai-slim, markdown-it-py, pydantic-settings are in pyproject.toml dependencies; pytest-asyncio is in dev deps"
    - "asyncio_mode = auto is set in [tool.pytest.ini_options]"
    - "tests/conftest.py has the block_real_llm_calls autouse fixture that sets pydantic_ai.models.ALLOW_MODEL_REQUESTS = False"
  artifacts:
    - path: "src/kir/core/ports/llm_port.py"
      provides: "Narrowed LLMPort Protocol + ExtractionResult structural Protocol"
      contains: "async def extract"
    - path: "src/kir/core/domain/models/document.py"
      provides: "Document model with diagnostics field"
      contains: "diagnostics: tuple[Diagnostic, ...]"
    - path: "src/kir/core/passes/context.py"
      provides: "Extended CompilerContext with Phase 2 fields"
      contains: "prompt_version"
    - path: "tests/compiler/documents/__init__.py"
      provides: "Test package init"
    - path: "tests/llm/__init__.py"
      provides: "Test package init"
  key_links:
    - from: "src/kir/core/ports/llm_port.py"
      to: "src/kir/core/domain/models/document.py"
      via: "Section import for LLMPort.extract() signature"
      pattern: "from kir.core.domain.models.document import Section"
    - from: "src/kir/core/passes/context.py"
      to: "src/kir/core/ports/cache_port.py"
      via: "llm_cache field typed as CachePort"
      pattern: "from kir.core.ports.cache_port import CachePort"
---

<objective>
Establish the Phase 2 contracts: narrow the LLMPort to the one-combined-call shape (D-02), fix the MarkdownParserPort return type (D-01 prerequisite), add the diagnostics field to Document (D-03 prerequisite), extend CompilerContext with the three new Phase 2 fields (prompt_version, llm_cache, prompts), install all required new dependencies, and wire up the test infrastructure (asyncio_mode, ALLOW_MODEL_REQUESTS guard).

Purpose: Every subsequent Phase 2 task implements against these contracts. Narrowing the port before writing any pass or adapter is the interface-first guarantee — no executor can accidentally build to the old extract(text) shape.

Output: Narrowed contracts in core/, extended CompilerContext, wired test infra, installed packages, empty test package __init__.py files.
</objective>

<execution_context>
@/Users/mniedre/.claude/gsd-core/workflows/execute-plan.md
@/Users/mniedre/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-document-compiler/02-CONTEXT.md
@.planning/phases/02-document-compiler/02-RESEARCH.md
@.planning/phases/02-document-compiler/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 dependencies and configure pytest async mode</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current deps and [tool.pytest.ini_options])
    - .planning/phases/02-document-compiler/02-RESEARCH.md (Standard Stack section, "To Add for Phase 2" table)
  </read_first>
  <action>
Run the following installs (production deps first, then dev):

  uv add "pydantic-ai-slim[openai,anthropic]>=2.0,<3" "markdown-it-py>=4.2.0" "pydantic-settings>=2"
  uv add --dev "pytest-asyncio>=0.21"

After installation, edit pyproject.toml to add asyncio_mode = "auto" under [tool.pytest.ini_options]. The current section only has testpaths = ["tests"]; add the asyncio_mode line to that same section.

Run uv run pytest -x -q to confirm the baseline still passes (exit 0 with zero tests collected is correct per the existing pytest_sessionfinish hook in tests/conftest.py).

Verify pydantic-ai-slim, markdown-it-py, pydantic-settings appear in [project].dependencies and pytest-asyncio appears in [dependency-groups].dev in the updated pyproject.toml.

Do NOT add pytest-recording in this task — it is optional per RESEARCH.md and not required by D-04's hand-crafted fixture approach.
  </action>
  <verify>
    <automated>grep -c "pydantic-ai-slim" pyproject.toml && grep -c "markdown-it-py" pyproject.toml && grep -c "asyncio_mode" pyproject.toml && uv run pytest -x -q</automated>
  </verify>
  <done>pyproject.toml contains pydantic-ai-slim, markdown-it-py, pydantic-settings in dependencies and pytest-asyncio in dev deps; asyncio_mode = "auto" is set; uv run pytest exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow LLMPort and MarkdownParserPort contracts</name>
  <files>
    src/kir/core/ports/llm_port.py,
    src/kir/core/ports/parser_port.py
  </files>
  <read_first>
    - src/kir/core/ports/llm_port.py (current: extract(text) -> object placeholder)
    - src/kir/core/ports/parser_port.py (current: parse(text) -> object placeholder)
    - src/kir/core/ports/cache_port.py (canonical Protocol pattern to replicate)
    - src/kir/core/domain/models/document.py (Section class to import for LLMPort signature)
    - .planning/phases/02-document-compiler/02-RESEARCH.md Pattern 1 (LLMPort narrowing)
    - .planning/phases/02-document-compiler/02-PATTERNS.md (llm_port.py section, ExtractionResult shape)
    - tests/core/passes/fakes/fake_llm_port.py (existing fake — its signature will need updating in tests)
    - tests/core/passes/fakes/fake_parser.py (existing fake — its parse() return type)
  </read_first>
  <action>
Rewrite src/kir/core/ports/llm_port.py with two Protocols:

1. ExtractionResult — a structural Protocol with four list-typed attributes (concepts, glossary, entities, references). Defined in this file so domain and pass code can type-hint against it without importing from llm/. Use from __future__ import annotations header.

2. LLMPort — a Protocol with:
   - model_id: str attribute
   - async def extract(self, *, sections: list[Section], prompt: str) -> ExtractionResult

Import Section from kir.core.domain.models.document. Use TYPE_CHECKING guard if needed to avoid circular imports (check actual import graph — document.py does not import from ports/, so a direct import is likely safe without TYPE_CHECKING).

Rewrite src/kir/core/ports/parser_port.py to change parse()'s return type from object to list[Section]. Import Section from kir.core.domain.models.document. Pattern mirrors cache_port.py but returns a domain type.

After editing both port files, run uv run pytest tests/core/ -x -q to verify all Phase 1 tests still pass. The existing FakeLLMPort in tests/core/passes/fakes/fake_llm_port.py has the old extract(text) -> object signature. Update it to match the new LLMPort shape: add model_id = "fake:v0" attribute and change to async def extract(self, *, sections, prompt: str) -> object (return object for now to avoid importing DocumentExtractionOutput before it exists). Also update FakeMarkdownParser in tests/core/passes/fakes/fake_parser.py to return list[Section] rather than object. Update tests/conftest.py's fake_compiler_context fixture if CompilerContext construction changes (it will once Task 3 adds the new fields with Optional defaults).
  </action>
  <acceptance_criteria>
    - src/kir/core/ports/llm_port.py contains "async def extract" with "sections: list[Section]" and "prompt: str" keyword-only args
    - src/kir/core/ports/llm_port.py defines "class ExtractionResult(Protocol):" with concepts, glossary, entities, references attributes
    - src/kir/core/ports/llm_port.py defines "model_id: str" on LLMPort Protocol
    - src/kir/core/ports/parser_port.py returns "list[Section]" not "object"
    - grep -r "def extract(self, text" src/kir/core/ returns zero matches (old signature removed)
    - uv run pytest tests/core/ -x -q passes (all Phase 1 tests green)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "async def extract" src/kir/core/ports/llm_port.py && grep -c "ExtractionResult" src/kir/core/ports/llm_port.py && uv run pytest tests/core/ -x -q</automated>
  </verify>
  <done>Both ports narrowed; no old extract(text) signature remains anywhere in src/kir/core/; all Phase 1 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Document model with diagnostics field and CompilerContext with Phase 2 fields; scaffold test packages</name>
  <files>
    src/kir/core/domain/models/document.py,
    src/kir/core/passes/context.py,
    tests/compiler/__init__.py,
    tests/compiler/documents/__init__.py,
    tests/llm/__init__.py,
    tests/conftest.py
  </files>
  <read_first>
    - src/kir/core/domain/models/document.py (current Document class without diagnostics)
    - src/kir/core/domain/ir.py (FakeIR — exact pattern for diagnostics field)
    - src/kir/core/domain/models/diagnostic.py (Diagnostic model to import)
    - src/kir/core/passes/context.py (current frozen dataclass with 5 fields)
    - src/kir/core/ports/cache_port.py (CachePort to import for llm_cache field)
    - tests/conftest.py (current fake_compiler_context fixture)
    - tests/core/domain/test_document.py (existing Document tests — must keep passing)
    - .planning/phases/02-document-compiler/02-PATTERNS.md (context.py and document.py sections)
  </read_first>
  <action>
Edit src/kir/core/domain/models/document.py:
- Add import: from kir.core.domain.models.diagnostic import Diagnostic
- Add field to Document class after the references field: diagnostics: tuple[Diagnostic, ...] = ()
- Keep all existing fields (id, title, source, checksum, language, sections, concepts, glossary, entities, references) unchanged with their existing types and defaults. Do not change the concepts/glossary/entities/references field types yet — they remain tuple[str, ...] = () for Phase 1 backward compatibility. Phase 2 passes will populate them via model_copy.

Edit src/kir/core/passes/context.py:
- Add import: from kir.core.ports.cache_port import CachePort
- Add three new optional fields to the frozen dataclass AFTER the existing five fields:
  prompt_version: str = ""
  llm_cache: CachePort | None = None
  prompts: object = None
- The prompts field is typed as object (not PromptRegistry) to avoid importing from llm/ into core/ — consistent with the hexagonal boundary.
- Keep @dataclass(frozen=True, slots=True) unchanged.
- All existing fields (llm, repository, parser, compiler_version, schema_version) remain in the same position.

Create empty __init__.py files:
- tests/compiler/__init__.py (empty, creates tests/compiler/ as a package)
- tests/compiler/documents/__init__.py (empty)
- tests/llm/__init__.py (empty)

Edit tests/conftest.py:
- Add import at top: from pydantic_ai import models as pydantic_ai_models
- Add the block_real_llm_calls autouse fixture per RESEARCH.md Pattern 5: an autouse=True session-scoped fixture that saves the original pydantic_ai_models.ALLOW_MODEL_REQUESTS value, sets it to False before yield, and restores it after.
- Keep the existing fake_compiler_context and fake_registry fixtures unchanged — they still work because the new CompilerContext fields all have defaults.

Run uv run pytest -x -q to confirm all existing tests still pass after these additions.
  </action>
  <acceptance_criteria>
    - grep "diagnostics" src/kir/core/domain/models/document.py returns a line with "tuple[Diagnostic, ...]"
    - grep "prompt_version" src/kir/core/passes/context.py shows the field with str default ""
    - grep "llm_cache" src/kir/core/passes/context.py shows the field with CachePort | None default None
    - grep "prompts" src/kir/core/passes/context.py shows the field with object type and None default
    - tests/compiler/__init__.py, tests/compiler/documents/__init__.py, tests/llm/__init__.py all exist as empty files
    - grep "ALLOW_MODEL_REQUESTS" tests/conftest.py shows the autouse fixture
    - uv run pytest -x -q passes (all existing Phase 1 tests green; new empty test packages are not an issue)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "diagnostics" src/kir/core/domain/models/document.py && grep -c "prompt_version" src/kir/core/passes/context.py && grep -c "ALLOW_MODEL_REQUESTS" tests/conftest.py && uv run pytest -x -q</automated>
  </verify>
  <done>Document has diagnostics field; CompilerContext has prompt_version, llm_cache, prompts fields with defaults; three empty test __init__.py files exist; conftest has ALLOW_MODEL_REQUESTS guard; all Phase 1 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml → uv install | External packages fetched from PyPI — supply chain entry point |
| test suite → pydantic_ai models | Any test that doesn't override with a fake could reach live API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | LLMPort narrowing | mitigate | ExtractionResult defined as structural Protocol in core/ — llm/ package satisfies it structurally; domain never imports pydantic_ai directly (Anti-Pattern 4) |
| T-02-02 | Information Disclosure | tests/conftest.py live API calls | mitigate | block_real_llm_calls autouse fixture sets pydantic_ai.models.ALLOW_MODEL_REQUESTS = False globally; any test that accidentally reaches live API fails loudly (LLM-03) |
| T-02-SC | Tampering | pydantic-ai-slim, markdown-it-py, pydantic-settings, pytest-asyncio installs | mitigate | RESEARCH.md Package Legitimacy Audit confirmed all four packages as Approved — pydantic-ai-slim (official Pydantic team), markdown-it-py (Google Assured OSS), pydantic-settings (official Pydantic team), pytest-asyncio (pytest-dev org). No slopcheck [SUS] packages. |
</threat_model>

<verification>
After all three tasks complete:
- uv run pytest -x -q passes (all existing Phase 1 tests green; new packages collected but empty is OK)
- grep -r "def extract(self, text" src/kir/core/ returns zero matches
- grep "async def extract" src/kir/core/ports/llm_port.py returns a match
- grep "diagnostics" src/kir/core/domain/models/document.py returns a match
- grep "prompt_version" src/kir/core/passes/context.py returns a match
- grep "asyncio_mode" pyproject.toml returns a match
- python -c "import pydantic_ai" exits 0 (confirms package installed)
- python -c "import markdown_it" exits 0 (confirms package installed)
</verification>

<success_criteria>
All Phase 1 tests pass. The four new packages are installed. The three port/model/context files have their narrowed/extended shapes. Test infrastructure is wired (asyncio_mode auto + ALLOW_MODEL_REQUESTS guard). All contracts that Phase 2 plans 02 and 03 build against are in place.
</success_criteria>

<output>
Create .planning/phases/02-document-compiler/02-01-SUMMARY.md when done.
</output>

## Artifacts This Phase Produces

| Symbol | File | Type |
|--------|------|------|
| ExtractionResult | src/kir/core/ports/llm_port.py | Protocol class |
| LLMPort (narrowed) | src/kir/core/ports/llm_port.py | Protocol class (replaces old) |
| MarkdownParserPort (narrowed) | src/kir/core/ports/parser_port.py | Protocol class (return type changed) |
| Document.diagnostics | src/kir/core/domain/models/document.py | field: tuple[Diagnostic, ...] = () |
| CompilerContext.prompt_version | src/kir/core/passes/context.py | field: str = "" |
| CompilerContext.llm_cache | src/kir/core/passes/context.py | field: CachePort \| None = None |
| CompilerContext.prompts | src/kir/core/passes/context.py | field: object = None |
| block_real_llm_calls | tests/conftest.py | autouse pytest fixture |
| tests/compiler/__init__.py | tests/compiler/__init__.py | empty package init |
| tests/compiler/documents/__init__.py | tests/compiler/documents/__init__.py | empty package init |
| tests/llm/__init__.py | tests/llm/__init__.py | empty package init |
</output>
