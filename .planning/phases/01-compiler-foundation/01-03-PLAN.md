---
phase: 01-compiler-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/kir/core/passes/base.py
  - src/kir/core/passes/registry.py
  - src/kir/core/passes/context.py
  - src/kir/core/config/versions.py
  - tests/core/passes/test_base.py
  - tests/core/passes/test_registry.py
  - tests/core/passes/test_context.py
autonomous: true
requirements: ["CORE-02", "CORE-03", "CORE-04", "CORE-05", "PASS-02", "PASS-05", "EXT-01"]
must_haves:
  truths:
    - "A new pass can be defined and registered via the @register_pass decorator in a brand-new file without editing registry.py, base.py, context.py, or any other existing pass file (EXT-01/CORE-03)"
    - "Calling PassRegistry.register() with a pass declaring depends_on naming an unregistered pass does NOT raise — the error only surfaces when PassRegistry.pipeline() is called (D-02)"
    - "PassRegistry.pipeline() returns passes in an order that respects every declared depends_on relationship, computed via graphlib.TopologicalSorter, not a hardcoded sequence"
    - "PassRegistry.pipeline() raises a clear, named error (MissingDependencyError or graphlib.CycleError) when a depends_on references an unregistered pass or when passes form a cycle, and the error message names the missing pass or cycle members"
    - "CompilerContext is an immutable, explicitly-constructed container (never a module-level global) carrying ports + run metadata, constructed once per test/run and passed into every pass call"
  artifacts:
    - path: "src/kir/core/passes/base.py"
      provides: "Pass Protocol: name, depends_on, __call__(ir, ctx) -> ir"
      contains: "class Pass(Protocol)"
    - path: "src/kir/core/passes/registry.py"
      provides: "PassRegistry with register() (never validates depends_on) and pipeline() (graphlib.TopologicalSorter-based, validates at build time per D-02)"
      contains: "class PassRegistry"
    - path: "src/kir/core/passes/context.py"
      provides: "CompilerContext frozen dataclass carrying llm/repository/parser ports + compiler_version/schema_version metadata"
      contains: "class CompilerContext"
    - path: "src/kir/core/config/versions.py"
      provides: "compiler_version, schema_version, prompt_version module-level constants"
      contains: "compiler_version"
    - path: "tests/core/passes/test_registry.py"
      provides: "D-02 build-time error detection proof (missing dependency + cycle), dependency-order proof, EXT-01 no-edit-existing-files proof"
      contains: "def test_missing_dependency_detected_at_pipeline_build_time"
  key_links:
    - from: "src/kir/core/passes/registry.py"
      to: "graphlib.TopologicalSorter"
      via: "stdlib import, used inside PassRegistry.pipeline()"
      pattern: "from graphlib import TopologicalSorter"
    - from: "src/kir/core/passes/context.py"
      to: "src/kir/core/ports/llm_port.py, repository_port.py, parser_port.py"
      via: "CompilerContext fields typed as LLMPort, RepositoryPort, MarkdownParserPort Protocols"
      pattern: "llm: .*LLMPort"
---

<objective>
Build the pass-registry mechanics: the `Pass` Protocol contract, the `PassRegistry` with decorator-based self-registration and `graphlib.TopologicalSorter`-based dependency-ordered pipeline construction, and the immutable `CompilerContext` dependency-injection container, satisfying CORE-02 through CORE-05, PASS-02, PASS-05, and EXT-01.

Purpose: This is the orchestration core every real compiler pass (Phase 2+) plugs into. The single largest concrete gap RESEARCH.md identified is that ARCHITECTURE.md's own sketch left `PassRegistry.pipeline()`'s body as a comment — this plan closes that gap with the real `graphlib`-based implementation, and proves D-02's "registration never validates, only pipeline() build does" contract with explicit build-time-vs-registration-time tests.
Output: `src/kir/core/passes/` (base.py, registry.py, context.py) and `src/kir/core/config/versions.py`, each proven by dedicated tests — registration/topo-order/cycle-detection/missing-dependency tests, plus a literal "add a new pass in a new file without touching existing files" proof for EXT-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-compiler-foundation/01-CONTEXT.md
@.planning/phases/01-compiler-foundation/01-RESEARCH.md
@.planning/phases/01-compiler-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass Protocol, CompilerContext, and version constants</name>
  <files>src/kir/core/passes/base.py, src/kir/core/passes/context.py, src/kir/core/config/versions.py, tests/core/passes/test_base.py, tests/core/passes/test_context.py</files>
  <read_first>
    - .planning/phases/01-compiler-foundation/01-PATTERNS.md (section "src/kir/core/passes/context.py" — exact recommendation: frozen dataclass NOT Pydantic BaseModel, with the shape-only sketch given; section header notes "No code sketch given in research — this is an explicit design decision the planner must make")
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (lines 241-244, the `Pass` Protocol shape: `name: str`, `depends_on: tuple[str, ...]`, `__call__(self, ir: object, ctx: "CompilerContext") -> object`)
    - src/kir/core/ports/llm_port.py, src/kir/core/ports/repository_port.py, src/kir/core/ports/parser_port.py (created by Plan 02 — these are the exact Protocol types CompilerContext's fields are typed against; read their method signatures before writing context.py's type hints)
  </read_first>
  <behavior>
    - Test 1: `Pass` is importable from `src.kir.core.passes.base` and is a `typing.Protocol` (assert it cannot be directly instantiated: `with pytest.raises(TypeError): Pass()`)
    - Test 2: `CompilerContext(llm=fake_llm, repository=fake_repo, parser=fake_parser, compiler_version="0.1.0", schema_version="1")` constructs successfully given fake port instances
    - Test 3: `CompilerContext` is frozen — assigning to any field after construction (`ctx.compiler_version = "x"`) raises `FrozenInstanceError` (dataclass) or equivalent
    - Test 4: Two `CompilerContext` instances constructed with identical port instances and identical version strings are NOT required to compare equal (Protocol-typed fields like port instances are not necessarily comparable) — this test only needs to confirm construction succeeds and fields are readable (`ctx.llm is fake_llm`), not equality semantics
    - Test 5: `compiler_version`, `schema_version`, `prompt_version` are importable string constants from `src.kir.core.config.versions`
  </behavior>
  <action>
    Create `src/kir/core/passes/base.py` with the `Pass` Protocol exactly per RESEARCH.md's sketch: `class Pass(Protocol): name: str; depends_on: tuple[str, ...]; def __call__(self, ir: object, ctx: "CompilerContext") -> object: ...` (use a `TYPE_CHECKING`-guarded import or string-quoted forward reference for `CompilerContext` to avoid a circular import between `base.py` and `context.py`). Create `src/kir/core/passes/context.py` with `CompilerContext` as `@dataclass(frozen=True, slots=True)` per PATTERNS.md's explicit recommendation (NOT a Pydantic BaseModel — Protocol-typed fields are not natively Pydantic-validatable without `arbitrary_types_allowed=True`, and CompilerContext is never serialized). Fields: `llm: LLMPort`, `repository: RepositoryPort`, `parser: MarkdownParserPort`, `compiler_version: str`, `schema_version: str`. Import the three port Protocol types from `src.kir.core.ports.llm_port`, `repository_port`, `parser_port` (these exist already from Plan 02 — both plans depend only on Plan 01's scaffolding, so import them directly, no stub needed). Create `src/kir/core/config/versions.py` with three module-level string constants: `compiler_version = "0.1.0"`, `schema_version = "1"`, `prompt_version = "1"` (prompt_version exists as a constant now even though no prompt exists until Phase 2 — CompilerContext/versions.py is the natural home per ARCHITECTURE.md's structure, and pre-declaring it avoids a later edit to this file when Phase 2 needs it).
  </action>
  <verify>
    <automated>uv run pytest tests/core/passes/test_base.py tests/core/passes/test_context.py -x -q</automated>
  </verify>
  <done>Pass Protocol and CompilerContext both exist, CompilerContext is frozen and explicitly constructed (no global), version constants are importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PassRegistry with graphlib-based topological pipeline and build-time error detection</name>
  <files>src/kir/core/passes/registry.py, tests/core/passes/test_registry.py</files>
  <read_first>
    - .planning/phases/01-compiler-foundation/01-PATTERNS.md (section "src/kir/core/passes/registry.py" — the complete, load-bearing verbatim implementation to copy, including MissingDependencyError and the graphlib.CycleError re-raise wrapping)
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (lines 504-524, Code Examples: "D-02 Build-Time Error Detection Proof" — exact test structure to follow for missing-dependency and cycle tests)
    - .planning/phases/01-compiler-foundation/01-CONTEXT.md (D-02 decision — registration never validates depends_on; only pipeline() validates, at build time)
    - src/kir/core/passes/base.py (created in Task 1 of this plan — the Pass Protocol type registry.py operates on)
  </read_first>
  <behavior>
    - Test 1: `registry.register(fake_pass_with_unregistered_dependency)` does NOT raise (D-02: registration always succeeds)
    - Test 2: Calling `registry.pipeline()` after registering a pass whose `depends_on` names an unregistered pass raises `MissingDependencyError` with the missing pass's name in the message (`match="nonexistent"` or similar)
    - Test 3: Registering pass "a" depends_on=("b",) and pass "b" depends_on=("a",), then calling `registry.pipeline()` raises `graphlib.CycleError`
    - Test 4: Registering pass "a" (no deps), pass "b" depends_on=("a",), pass "c" depends_on=("a","b") in any registration order, `registry.pipeline()` returns a list where "a" precedes "b" and both precede "c" (assert via index comparison, not exact list equality, since topological sort may not be unique when ties exist)
    - Test 5 (EXT-01 proof): A NEW pass defined and registered entirely within the test file itself (not editing registry.py/base.py) successfully appears in `registry.pipeline()`'s output — demonstrating a third party can add a pass without modifying any existing pass-mechanics file
  </behavior>
  <action>
    Create `src/kir/core/passes/registry.py` with `MissingDependencyError(ValueError)` and `PassRegistry` exactly per PATTERNS.md's verbatim sketch: `__init__` creates `self._passes: dict[str, Pass] = {}`; `register(self, pass_obj: Pass) -> Pass` inserts into `self._passes` keyed by `pass_obj.name` and never inspects `depends_on` (D-02); `pipeline(self) -> list[Pass]` builds a `graph: dict[str, set[str]]` from every registered pass's `depends_on`, raising `MissingDependencyError` per-dependency if a referenced pass name is absent from `self._passes`, then runs `graphlib.TopologicalSorter(graph).static_order()` wrapped in a `try/except CycleError` that re-raises a `CycleError` with a message naming `exc.args[1]` (the actual cycle nodes, per the CPython docs cited in RESEARCH.md), and finally returns `[self._passes[name] for name in ordered_names]`. Each `PassRegistry` instance used in tests must be constructed fresh per test (not a shared module-level singleton across tests) to avoid cross-test pass-name collisions — use a pytest fixture or local instantiation per test function.
  </action>
  <verify>
    <automated>uv run pytest tests/core/passes/test_registry.py -x -q</automated>
  </verify>
  <done>PassRegistry.register() never validates depends_on; PassRegistry.pipeline() validates via graphlib.TopologicalSorter at build time, raising MissingDependencyError (named pass) or CycleError (named cycle) as appropriate; a new pass can be registered and ordered without editing any existing pass-mechanics file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pass registration (decorator self-registration) | Any code that imports a decorated pass module can register a pass into the module-level registry — this is an intra-process, single-author-codebase boundary, not a multi-tenant/untrusted-plugin boundary this phase (per EXT-01's resolved scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `PassRegistry.register()` accepting a malformed (non-Pass-shaped) object | accept | Not a realistic threat in this single-process, internal-API context this phase — no untrusted plugin loading exists in v1 per EXT-01's resolved scope (RESEARCH.md Security Domain explicitly notes this is a low-priority structural-validation nicety, not a blocking control). No mitigation task added this plan; flagged for future reconsideration only if EXT-01 ever extends to out-of-repo third-party plugin distribution. |
| T-01-06 | Denial of Service | Circular pass dependency causing `PassRegistry.pipeline()` to hang or loop indefinitely | mitigate | `graphlib.TopologicalSorter` raises `CycleError` immediately on `static_order()` rather than looping — stdlib-guaranteed behavior, proven by Task 2's Test 3 |
| T-01-07 | Tampering | `PassRegistry` as sanctioned module-level state (the one approved global per CLAUDE.md's "No global state" caveat) | accept | RESEARCH.md explicitly sanctions this exception (decorator self-registration requires a module-level registry target); mitigated structurally by keeping `CompilerContext` — the actual state-carrying object passes operate on — always explicitly constructed and passed, never global |
</threat_model>

<verification>
`uv run pytest tests/core/passes/test_base.py tests/core/passes/test_context.py tests/core/passes/test_registry.py -x -q` — Pass Protocol, CompilerContext, and PassRegistry (registration, topological ordering, build-time error detection, EXT-01 no-edit proof) all pass.
</verification>

<success_criteria>
A new pass can be registered and correctly ordered in a brand-new file without editing registry.py/base.py/context.py. Dependency-graph errors (missing dependency, cycle) are detected only at pipeline() build time, never at register() time, each with a clear named error. CompilerContext is immutable and explicitly constructed.
</success_criteria>

## Artifacts this phase produces

- `src/kir/core/passes/base.py` — `Pass` Protocol (`name`, `depends_on`, `__call__(ir, ctx) -> ir`)
- `src/kir/core/passes/context.py` — `CompilerContext` frozen dataclass (fields: `llm`, `repository`, `parser`, `compiler_version`, `schema_version`)
- `src/kir/core/passes/registry.py` — `MissingDependencyError` exception class, `PassRegistry` class (`register()`, `pipeline()`)
- `src/kir/core/config/versions.py` — `compiler_version`, `schema_version`, `prompt_version` string constants
- `tests/core/passes/test_base.py`, `test_context.py`, `test_registry.py` — Protocol/construction/topological-order/build-time-error test suites

<output>
Create `.planning/phases/01-compiler-foundation/01-03-SUMMARY.md` when done
</output>
