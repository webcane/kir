---
phase: 01-compiler-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - src/kir/__init__.py
  - src/kir/core/__init__.py
  - src/kir/core/domain/__init__.py
  - src/kir/core/domain/models/__init__.py
  - src/kir/core/ports/__init__.py
  - src/kir/core/passes/__init__.py
  - src/kir/core/config/__init__.py
  - src/kir/tooling/__init__.py
  - src/kir/tooling/repository/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/core/__init__.py
  - tests/core/domain/__init__.py
  - tests/core/ports/__init__.py
  - tests/core/passes/__init__.py
  - tests/core/passes/fakes/__init__.py
  - tests/tooling/__init__.py
  - tests/tooling/repository/__init__.py
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `uv run pytest` from repo root succeeds (collects zero tests, exits 0) proving the toolchain, package layout, and test discovery all work end-to-end before any domain code is written"
    - "The package is installed in editable mode so `import kir` resolves from `src/kir/` without path hacks in test files"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed project definition: name=kir, src layout, pydantic + pytest dependencies, [tool.pytest.ini_options] with testpaths=tests and src/ on path via package install"
      contains: "[tool.pytest.ini_options]"
    - path: "src/kir/core/__init__.py"
      provides: "core package init (empty, marks package)"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures placeholder — fake_compiler_context, fake_registry, tmp_path-based repository fixture stubs wired up in Plan 04, but the file and its import surface exist now so later plans only add fixtures, never create the file"
  key_links: []
---

<objective>
Scaffold the KIR Python project from zero: `pyproject.toml` (uv-managed, src layout), the full empty package/test directory skeleton for the 5-package architecture's `core` and `tooling` packages (per `.planning/research/ARCHITECTURE.md` "Recommended Project Structure"), and a working `uv run pytest` baseline that collects zero tests and exits 0.

Purpose: This is a from-scratch repository (no `src/`, no `pyproject.toml`, no installed dependencies) — every later plan in this phase assumes `uv run pytest` already works and `import kir.core...` already resolves. Without this wave, no other plan in Phase 1 can run a single test.
Output: A `uv`-managed Python 3.13 project with pydantic + pytest installed, an empty-but-importable `src/kir/core/` and `src/kir/tooling/` package tree, and an empty-but-collectible `tests/` tree mirroring it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-compiler-foundation/01-RESEARCH.md
@.planning/phases/01-compiler-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize uv project and install pydantic + pytest</name>
  <files>pyproject.toml, .python-version, .gitignore</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Recommended Project Structure section, lines 68-127 — the exact 5-package src/kir/ layout this scaffolding must support)
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (Standard Stack section — pydantic 2.13.x, pytest 8.x, "Installation" bash block: `uv add "pydantic>=2.13"`, `uv add --dev "pytest>=8"`)
  </read_first>
  <action>
    Run `uv init --package --name kir --python 3.13` at the repo root (or hand-write pyproject.toml if `uv init` conflicts with existing CLAUDE.md/LICENSE/README.md — do not overwrite README.md or LICENSE). Ensure the resulting layout uses `src/kir/` (src-layout, not flat-layout) — pass `uv init --package` which defaults to src-layout for `--package` mode, and verify `pyproject.toml`'s `[tool.hatchling.build.targets.wheel]` or build-backend config points at `src/kir`. Set `requires-python = ">=3.13"`. Add dependency `pydantic>=2.13` via `uv add "pydantic>=2.13"`. Add dev dependency `pytest>=8` via `uv add --dev "pytest>=8"`. Add `[tool.pytest.ini_options]` to pyproject.toml with `testpaths = ["tests"]` and `pythonpath` NOT needed if the package installs in editable mode (verify `uv sync` creates an editable install — check for `kir.egg-link` or `__editable__` entries, or confirm via `uv run python -c "import kir; print(kir.__file__)"` resolving to `src/kir/__init__.py`). Create `.python-version` pinning `3.13`. Create or extend `.gitignore` to include `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `uv.lock` is NOT gitignored (commit it — uv.lock should be tracked for reproducibility).
  </action>
  <verify>
    <automated>uv run python -c "import kir" && test -f uv.lock && test -f pyproject.toml</automated>
  </verify>
  <done>pyproject.toml declares name=kir, requires-python>=3.13, pydantic>=2.13, pytest>=8 dev dep, and [tool.pytest.ini_options] testpaths=["tests"]; uv.lock is tracked; `uv run python -c "import kir"` exits 0.</done>
  <acceptance_criteria>
    - `pyproject.toml` exists at repo root with `name = "kir"`, `requires-python = ">=3.13"`, a `pydantic` dependency `>=2.13`, and a `pytest` dev dependency `>=8`
    - `pyproject.toml` contains a `[tool.pytest.ini_options]` section with `testpaths = ["tests"]`
    - `uv run python -c "import kir"` exits 0 with no output (proves editable install resolves `src/kir/`)
    - `uv.lock` exists and is tracked (not in `.gitignore`)
    - `.gitignore` contains `.venv/` and `__pycache__/`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create empty package and test directory skeleton with working pytest collection</name>
  <files>
    src/kir/__init__.py, src/kir/core/__init__.py, src/kir/core/domain/__init__.py, src/kir/core/domain/models/__init__.py, src/kir/core/ports/__init__.py, src/kir/core/passes/__init__.py, src/kir/core/config/__init__.py, src/kir/tooling/__init__.py, src/kir/tooling/repository/__init__.py,
    tests/__init__.py, tests/conftest.py, tests/core/__init__.py, tests/core/domain/__init__.py, tests/core/ports/__init__.py, tests/core/passes/__init__.py, tests/core/passes/fakes/__init__.py, tests/tooling/__init__.py, tests/tooling/repository/__init__.py
  </files>
  <read_first>
    - pyproject.toml (just created in Task 1 — confirm [tool.pytest.ini_options] testpaths value before adding test files)
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (Recommended Project Structure code block, lines 178-226 — exact directory tree this task must instantiate as empty `__init__.py` files)
    - .planning/phases/01-compiler-foundation/01-VALIDATION.md (Wave 0 Requirements section — tests/conftest.py shared fixtures requirement)
  </read_first>
  <action>
    Create every directory and `__init__.py` file listed in the `<files>` field above, all empty except where noted. Each `__init__.py` is a zero-byte or single-comment placeholder (e.g. a one-line module docstring naming the package, no logic). Create `tests/conftest.py` with a docstring noting fixtures are added in Plan 04 (`fake_compiler_context`, `fake_registry`, `tmp_path`-based repository fixture) — leave the file otherwise empty (no fixture functions yet; Plan 04 adds them, this task only ensures the file exists so pytest's rootdir-relative conftest discovery is established from Wave 0). Do not create `tests/core/passes/fakes/fake_passes.py`, `fake_llm_port.py`, `fake_repository.py`, or `fake_parser.py` here — those belong to later plans; this task creates only the directory (`tests/core/passes/fakes/__init__.py`) so the package path exists.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q</automated>
  </verify>
  <done>uv run pytest exits 0 with zero collection errors and zero tests collected; every package directory under src/kir/ and tests/ contains a valid __init__.py.</done>
  <acceptance_criteria>
    - `uv run pytest` from repo root exits 0 and reports "no tests ran" (zero collection errors)
    - `uv run pytest --collect-only` produces no `ERROR` lines (proves every `__init__.py` is syntactically valid Python and every package path is importable)
    - `src/kir/core/domain/models/__init__.py`, `src/kir/core/ports/__init__.py`, `src/kir/core/passes/__init__.py`, `src/kir/core/config/__init__.py`, `src/kir/tooling/repository/__init__.py` all exist
    - `tests/conftest.py` exists and is a valid (importable) Python file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None this plan | Pure project scaffolding — no runtime code, no I/O beyond local file creation by the developer/executor, no external input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | uv.lock / dependency resolution | mitigate | Pin `pydantic>=2.13` and `pytest>=8` exactly as researched in 01-RESEARCH.md's Package Legitimacy Audit (both packages pre-vetted, mature, high-download PyPI packages); commit `uv.lock` so dependency resolution is reproducible and not silently re-resolved to a different (potentially compromised) version on a later `uv sync` |
| T-01-SC | Tampering | npm/pip/uv installs | accept | No new untrusted packages introduced this plan — pydantic and pytest were already audited in `.planning/research/STACK.md` and re-confirmed in `01-RESEARCH.md`'s Package Legitimacy Audit table as "Approved (carried over, not re-audited)"; no `[ASSUMED]`/`[SUS]` packages exist in this plan's scope, so no blocking human-verify checkpoint is required |
</threat_model>

<verification>
`uv run pytest` exits 0 with zero collection errors. `uv run python -c "import kir"` exits 0. `pyproject.toml` and `uv.lock` both exist and are committed.
</verification>

<success_criteria>
A clean checkout of this commit, followed by `uv sync` and `uv run pytest`, succeeds with zero errors and zero tests collected — proving the toolchain baseline every subsequent Phase 1 plan depends on.
</success_criteria>

## Artifacts this phase produces

- `pyproject.toml` — project manifest (name=kir, requires-python>=3.13, pydantic/pytest deps, `[tool.pytest.ini_options]`)
- `.python-version` — pins Python 3.13
- `.gitignore` — adds `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`
- `uv.lock` — locked dependency resolution
- `src/kir/__init__.py`, `src/kir/core/__init__.py`, `src/kir/core/domain/__init__.py`, `src/kir/core/domain/models/__init__.py`, `src/kir/core/ports/__init__.py`, `src/kir/core/passes/__init__.py`, `src/kir/core/config/__init__.py`, `src/kir/tooling/__init__.py`, `src/kir/tooling/repository/__init__.py` — empty package markers establishing the 5-package `core`/`tooling` skeleton
- `tests/__init__.py`, `tests/conftest.py` (empty placeholder), `tests/core/__init__.py`, `tests/core/domain/__init__.py`, `tests/core/ports/__init__.py`, `tests/core/passes/__init__.py`, `tests/core/passes/fakes/__init__.py`, `tests/tooling/__init__.py`, `tests/tooling/repository/__init__.py` — empty test package markers mirroring the src tree

<output>
Create `.planning/phases/01-compiler-foundation/01-01-SUMMARY.md` when done
</output>
