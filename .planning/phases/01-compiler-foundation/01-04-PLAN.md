---
phase: 01-compiler-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-02", "01-03"]
files_modified:
  - src/kir/tooling/repository/yaml_repository.py
  - tests/core/passes/fakes/fake_passes.py
  - tests/core/passes/fakes/fake_llm_port.py
  - tests/core/passes/fakes/fake_repository.py
  - tests/core/passes/fakes/fake_parser.py
  - tests/core/passes/fakes/__init__.py
  - tests/conftest.py
  - tests/core/test_repository_port_contract.py
  - tests/core/passes/test_pipeline_execution.py
  - tests/tooling/repository/test_yaml_repository.py
autonomous: true
requirements: ["CORE-02", "CORE-04", "CORE-06", "PASS-01", "PASS-03", "PASS-04", "STOR-01", "STOR-02"]
must_haves:
  truths:
    - "A fake LLMPort, fake RepositoryPort (two interchangeable variants — in-memory and YAML-file-backed), and fake MarkdownParserPort each satisfy their respective port contracts and are swappable in CompilerContext without any domain or pass code change"
    - "Running the same set of fake passes twice against the same fake inputs produces byte-identical serialized output, and each pass's output includes structured Diagnostics rather than printed/logged side effects"
    - "Writing an artifact through the YAML repository port produces one individual YAML file per artifact (no monolithic JSON), in a directory that is verifiably separate from any raw-source directory"
    - "A single shared contract test runs unmodified against both the in-memory fake repository and the YAML-file-backed repository, proving port-substitutability rather than merely asserting a fake exists"
  artifacts:
    - path: "src/kir/tooling/repository/yaml_repository.py"
      provides: "YamlFileRepository implementing RepositoryPort — one YAML file per artifact via ruamel.yaml, real (permanent) adapter per Open Question 2's resolution"
      contains: "class YamlFileRepository"
    - path: "tests/core/passes/fakes/fake_passes.py"
      provides: "Two-plus fake CompilerPass implementations (fake_pass_a, fake_pass_b depends_on fake_pass_a) exercising a real dependency graph"
      contains: "def fake_pass_a"
    - path: "tests/core/passes/fakes/fake_llm_port.py"
      provides: "FakeLLMPort satisfying LLMPort"
      contains: "class FakeLLMPort"
    - path: "tests/core/passes/fakes/fake_repository.py"
      provides: "InMemoryFakeRepository satisfying RepositoryPort (variant 1)"
      contains: "class InMemoryFakeRepository"
    - path: "tests/core/passes/fakes/fake_parser.py"
      provides: "FakeMarkdownParser satisfying MarkdownParserPort"
      contains: "class FakeMarkdownParser"
    - path: "tests/core/test_repository_port_contract.py"
      provides: "Parametrized contract test run against both InMemoryFakeRepository and YamlFileRepository"
      contains: "def test_save_then_load_roundtrips"
    - path: "tests/core/passes/test_pipeline_execution.py"
      provides: "Byte-identical rerun proof (success criterion 3) + diagnostics-accumulation/never-halts proof (D-01)"
      contains: "def test_rerun_produces_byte_identical_output"
    - path: "tests/tooling/repository/test_yaml_repository.py"
      provides: "STOR-01 (one file per artifact) and STOR-02 (output dir disjoint from raw dir) proofs"
      contains: "def test_one_file_per_artifact"
  key_links:
    - from: "tests/core/passes/fakes/fake_passes.py"
      to: "src/kir/core/passes/registry.py"
      via: "@register_pass decorator registering into a PassRegistry instance"
      pattern: "register_pass\\("
    - from: "src/kir/tooling/repository/yaml_repository.py"
      to: "src/kir/core/ports/repository_port.py"
      via: "structural Protocol satisfaction (save/load methods)"
      pattern: "def save.*def load|def load.*def save"
    - from: "tests/conftest.py"
      to: "src/kir/core/passes/context.py"
      via: "fake_compiler_context fixture constructing CompilerContext with fake ports"
      pattern: "def fake_compiler_context"
---

<objective>
Prove the entire Phase 1 mechanism end-to-end with fakes: build the fake LLMPort/RepositoryPort/MarkdownParserPort implementations and fake passes exercising a real dependency graph, the real (permanent) YAML-file-backed repository adapter, and the integration tests that prove byte-identical reruns, diagnostics accumulation, port-substitutability via shared contract tests, and one-YAML-file-per-artifact storage with raw/output directory separation.

Purpose: This plan is where Phase 1's five ROADMAP.md success criteria become provable facts, not just available mechanisms — it wires together everything Plan 02 (domain/ports) and Plan 03 (registry/context) built and demonstrates the full proof: fake passes register, order correctly, execute deterministically, accumulate structured diagnostics without halting, and a real filesystem-backed repository adapter satisfies STOR-01/STOR-02.
Output: `tests/core/passes/fakes/` (4 fake adapter/pass modules), `src/kir/tooling/repository/yaml_repository.py` (the first permanent adapter in the project), and 3 integration/contract test files proving the phase's success criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-compiler-foundation/01-CONTEXT.md
@.planning/phases/01-compiler-foundation/01-RESEARCH.md
@.planning/phases/01-compiler-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fake ports, fake passes, shared conftest fixtures, and the byte-identical rerun + diagnostics proof</name>
  <files>
    tests/core/passes/fakes/fake_passes.py, tests/core/passes/fakes/fake_llm_port.py, tests/core/passes/fakes/fake_repository.py, tests/core/passes/fakes/fake_parser.py, tests/core/passes/fakes/__init__.py,
    tests/conftest.py, tests/core/passes/test_pipeline_execution.py
  </files>
  <read_first>
    - .planning/phases/01-compiler-foundation/01-PATTERNS.md (section "tests/core/passes/fakes/fake_passes.py" — exact register_pass decorator + fake_pass_a/fake_pass_b sketch; section "tests/core/passes/test_pipeline_execution.py" — exact byte-identical rerun test to follow)
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (lines 426-430, Pitfall 1: decorator registration never fires unless the module is imported — the __init__.py mitigation is mandatory here; lines 432-436, Pitfall 2: byte-identical reruns fail from unsorted collection iteration, not Pydantic serialization)
    - .planning/phases/01-compiler-foundation/01-CONTEXT.md (D-01 — pipeline always runs every pass to completion regardless of diagnostic severity; diagnostics accumulate, never halt mid-run)
    - src/kir/core/passes/registry.py (created by Plan 03 — PassRegistry.register()/pipeline() this task's fakes register into)
    - src/kir/core/passes/context.py (created by Plan 03 — CompilerContext shape the fake_compiler_context fixture constructs)
    - src/kir/core/domain/ir.py (created by Plan 02 — FakeIR type the fake passes operate on)
    - src/kir/core/ports/llm_port.py, repository_port.py, parser_port.py (created by Plan 02 — exact Protocol method signatures fakes must satisfy)
  </read_first>
  <behavior>
    - Test 1: A fresh `PassRegistry` with `fake_pass_a` (no deps) and `fake_pass_b` (depends_on=("fake_a",)) registered, calling `.pipeline()` and executing each pass in order against a `FakeIR(value=0)` input, produces a final `FakeIR` with `diagnostics` containing at least 2 entries (one contributed by each pass) and `value` reflecting both passes having run
    - Test 2: Running the exact same pipeline twice against two fresh `FakeIR(value=0)` inputs (not reusing mutated state) produces `result_1.model_dump_json() == result_2.model_dump_json()` (byte-identical)
    - Test 3: Every diagnostic in the final result's `diagnostics` tuple is an instance of `Diagnostic` (structured), and no test asserts on captured stdout/stderr output (proving CORE-06's "never print/log" contract is upheld by construction, not just by absence of print statements)
    - Test 4: `FakeLLMPort`, `InMemoryFakeRepository`, `FakeMarkdownParser` each construct with no arguments (or trivial arguments) and their methods are callable and return values matching their Protocol's declared return type shape
    - Test 5: A `CompilerContext` constructed via the `fake_compiler_context` fixture with `FakeLLMPort()`, `InMemoryFakeRepository()`, `FakeMarkdownParser()` successfully passes into a fake pass call (`fake_pass_a(ir, ctx)`) without error, proving CompilerContext + ports + passes compose end-to-end
  </behavior>
  <action>
    Create `tests/core/passes/fakes/fake_llm_port.py` with `FakeLLMPort` — a plain class with whatever method(s) `LLMPort` declares (read the Protocol from Plan 02's `llm_port.py` first), returning a trivial canned value, no real LLM call. Create `tests/core/passes/fakes/fake_repository.py` with `InMemoryFakeRepository` exactly per PATTERNS.md's sketch: `__init__` creates `self._store: dict[str, object] = {}`; `save(self, artifact_id, artifact)` stores into the dict; `load(self, artifact_id)` returns from the dict. Create `tests/core/passes/fakes/fake_parser.py` with `FakeMarkdownParser` satisfying `MarkdownParserPort`'s declared method, returning a trivial canned value. Create `tests/core/passes/fakes/fake_passes.py` per PATTERNS.md's sketch: a module-level `PassRegistry()` instance, a local `register_pass(name, depends_on=())` decorator that attaches `fn.name`/`fn.depends_on` as function attributes and calls the module registry's `.register(fn)`, then `fake_pass_a` (no deps, returns `ir.model_copy(update={"diagnostics": ir.diagnostics + (Diagnostic(code="FAKE_A", severity=Severity.INFO, message="fake_pass_a ran"),), "value": ir.value + 1})`) and `fake_pass_b` (depends_on=("fake_a",), same pattern incrementing `value` and appending a distinct `Diagnostic`). Create `tests/core/passes/fakes/__init__.py` importing every fake module (`fake_passes`, `fake_llm_port`, `fake_repository`, `fake_parser`) so decorator registration always fires regardless of test collection order (Pitfall 1 mitigation — mandatory, not optional). Extend `tests/conftest.py` (created empty by Plan 01) adding pytest fixtures: `fake_compiler_context` (constructs and returns a `CompilerContext` using `FakeLLMPort()`, `InMemoryFakeRepository()`, `FakeMarkdownParser()`, and the version constants from `src.kir.core.config.versions`), `fake_registry` (returns the module-level registry from `fake_passes.py`, or constructs a fresh `PassRegistry` with `fake_pass_a`/`fake_pass_b` registered if test isolation requires a fresh instance per test — prefer fresh-per-test construction over reusing a shared module-level registry across tests to avoid cross-test interference). Create `tests/core/passes/test_pipeline_execution.py` per PATTERNS.md's byte-identical rerun sketch, executing the full pipeline twice from fresh `FakeIR(value=0)` instances and asserting `model_dump_json()` equality, plus asserting every diagnostic in the result is a `Diagnostic` instance.
  </action>
  <verify>
    <automated>uv run pytest tests/core/passes/ -x -q</automated>
  </verify>
  <done>Two fake passes with a real dependency (fake_b depends_on fake_a) execute via the registry pipeline, produce byte-identical reruns, and accumulate structured Diagnostic instances without ever printing/logging; all fake ports construct and are usable inside a CompilerContext.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: YAML-file repository adapter, shared port contract test, STOR-01/STOR-02 proofs</name>
  <files>src/kir/tooling/repository/yaml_repository.py, tests/core/test_repository_port_contract.py, tests/tooling/repository/test_yaml_repository.py</files>
  <read_first>
    - .planning/phases/01-compiler-foundation/01-PATTERNS.md (section "tests/core/passes/fakes/fake_repository.py + src/kir/tooling/repository/yaml_repository.py" — exact YamlFileRepository sketch and the parametrized contract test sketch; "Security note to carry into implementation" — artifact_id path-traversal sanitization)
    - .planning/phases/01-compiler-foundation/01-RESEARCH.md (Security Domain section, "Path traversal via unvalidated artifact_id" row — the concrete mitigation required: derive artifact_id from a slug/UUID pattern, validate before constructing a filesystem path)
    - tests/core/passes/fakes/fake_repository.py (created in Task 1 of this plan — InMemoryFakeRepository, the other half of the shared contract test's parametrization)
    - src/kir/core/ports/repository_port.py (created by Plan 02 — the exact save/load signature YamlFileRepository must satisfy)
  </read_first>
  <behavior>
    - Test 1: `YamlFileRepository(tmp_path / "kir").save("artifact-1", {"id": "artifact-1", "version": 1})` followed by `.load("artifact-1")` returns `{"id": "artifact-1", "version": 1}` (round-trip)
    - Test 2: After saving artifacts "artifact-1" and "artifact-2", the output directory contains exactly 2 `.yaml` files (one per artifact, not one combined JSON/YAML file) — assert via `list(output_dir.glob("*.yaml"))` length
    - Test 3: The repository's output directory (e.g. `tmp_path / "kir"`) and a separately-constructed "raw source" directory (e.g. `tmp_path / "raw"`) are disjoint — no file written by `.save()` appears under the raw directory, and no raw-source file is read/modified by `.save()`/`.load()`
    - Test 4: Calling `YamlFileRepository(...).save(artifact_id, artifact)` with an `artifact_id` containing path-traversal characters (e.g. `"../../etc/passwd"`) either raises a validation error OR is sanitized such that no file is written outside the configured output directory — assert via checking no file exists outside `output_dir` after the call (whichever behavior is implemented, the test must prove the traversal does not escape the output directory)
    - Test 5 (contract test, parametrized over both `InMemoryFakeRepository` and `YamlFileRepository`): `test_save_then_load_roundtrips` runs unmodified against both fixture params and passes for both
  </behavior>
  <action>
    Create `src/kir/tooling/repository/yaml_repository.py` with `YamlFileRepository` per PATTERNS.md's sketch: `__init__(self, output_dir: Path)` stores `self._dir = output_dir` and constructs `self._yaml = ruamel.yaml.YAML(typ="safe")`; `save(self, artifact_id: str, artifact: object) -> None` validates `artifact_id` is a safe filename component before use (reject or strip any `/`, `\`, or `..` segment — concretely: assert `artifact_id` matches a restrictive pattern such as `^[A-Za-z0-9_-]+$`, raising `ValueError` if it does not, per the Security Domain path-traversal mitigation), creates `self._dir` via `mkdir(parents=True, exist_ok=True)`, and writes `self._dir / f"{artifact_id}.yaml"` via `self._yaml.dump(artifact, f)`; `load(self, artifact_id: str) -> object` reads and returns the parsed YAML from the same path (apply the same `artifact_id` validation before constructing the load path). Add `ruamel.yaml>=0.19` as a project dependency via `uv add "ruamel.yaml>=0.19"` if not already present from Plan 01 (Plan 01 only added pydantic/pytest — this task is responsible for adding ruamel.yaml since it's the first file that needs it). Create `tests/core/test_repository_port_contract.py` per PATTERNS.md's exact parametrized fixture sketch (`@pytest.fixture(params=["in_memory", "yaml_file"])`), importing `InMemoryFakeRepository` from `tests.core.passes.fakes.fake_repository` and `YamlFileRepository` from `src.kir.tooling.repository.yaml_repository`. Create `tests/tooling/repository/test_yaml_repository.py` with the STOR-01 (one-file-per-artifact count assertion) and STOR-02 (output/raw directory disjointness assertion) tests, plus the path-traversal `artifact_id` test.
  </action>
  <verify>
    <automated>uv run pytest tests/tooling/repository/ tests/core/test_repository_port_contract.py -x -q</automated>
  </verify>
  <done>YamlFileRepository writes one YAML file per artifact to a directory disjoint from any raw-source directory, rejects/sanitizes path-traversal artifact_ids, and the same contract test passes unmodified against both InMemoryFakeRepository and YamlFileRepository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `YamlFileRepository.save()`/`.load()` artifact_id -> filesystem path | `artifact_id` is the only externally-influenced input this phase that becomes a filesystem path component — untrusted in the sense that a future caller (a real pass output, Phase 2+) could construct it from LLM-derived or document-derived content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering / Information Disclosure | `YamlFileRepository.save()`/`.load()` — `artifact_id` used directly as a filename | mitigate | Validate `artifact_id` against a restrictive allowlist pattern (`^[A-Za-z0-9_-]+$`) before constructing any `Path`, raising `ValueError` on violation — implemented in Task 2, proven by Task 2's Test 4 (path-traversal artifact_id test). This is the one concrete filesystem-write code path in Phase 1 and the only place ASVS V5 input validation has real teeth this phase. |
| T-01-09 | Tampering | `ruamel.yaml.YAML(typ="safe")` mode selection | mitigate | Explicitly use `typ="safe"` (not the default round-trip-preserving mode, which can be more permissive about constructing arbitrary Python objects from YAML tags) — `typ="safe"` restricts deserialization to plain Python types (dict/list/str/int/etc.), preventing arbitrary object construction from a malicious YAML file |
| T-01-10 | Tampering | npm/pip/uv installs (`ruamel.yaml` newly added this plan) | accept | Already vetted in `.planning/research/STACK.md` and re-confirmed in `01-RESEARCH.md`'s Standard Stack table ("Already locked in STACK.md for git-diff-friendly, round-trip-faithful YAML") — not a new/unaudited package, no blocking checkpoint required |
</threat_model>

<verification>
`uv run pytest -x -q` (full suite, all three plans' tests combined) — every Phase 1 success criterion from ROADMAP.md is proven: dependency-ordered fake pass execution, byte-identical reruns, structured diagnostics accumulation, port-substitutability via shared contract tests across 2+ fakes per port, and one-YAML-file-per-artifact storage disjoint from raw source.
</verification>

<success_criteria>
The full Phase 1 test suite (`uv run pytest -x -q`) is green. A fake LLMPort, fake RepositoryPort (2 variants), and fake MarkdownParserPort all satisfy their ports and swap into CompilerContext without domain/pass code changes. Fake pipeline reruns are byte-identical. YamlFileRepository produces one file per artifact in a directory disjoint from raw source, with path-traversal-safe artifact_id handling.
</success_criteria>

## Artifacts this phase produces

- `src/kir/tooling/repository/yaml_repository.py` — `YamlFileRepository` class (`save`, `load`); the project's first permanent adapter
- `tests/core/passes/fakes/fake_passes.py` — module-level `PassRegistry` instance, `register_pass()` decorator, `fake_pass_a`, `fake_pass_b` functions
- `tests/core/passes/fakes/fake_llm_port.py` — `FakeLLMPort` class
- `tests/core/passes/fakes/fake_repository.py` — `InMemoryFakeRepository` class
- `tests/core/passes/fakes/fake_parser.py` — `FakeMarkdownParser` class
- `tests/conftest.py` — `fake_compiler_context` fixture, `fake_registry` fixture (extending the placeholder file from Plan 01)
- `tests/core/test_repository_port_contract.py` — `repository` parametrized fixture, `test_save_then_load_roundtrips()`
- `tests/core/passes/test_pipeline_execution.py` — `test_rerun_produces_byte_identical_output()` and structured-diagnostics assertions
- `tests/tooling/repository/test_yaml_repository.py` — `test_one_file_per_artifact()`, `test_output_dir_disjoint_from_raw()`, path-traversal `artifact_id` test
- New dependency: `ruamel.yaml>=0.19` added to `pyproject.toml`

<output>
Create `.planning/phases/01-compiler-foundation/01-04-SUMMARY.md` when done
</output>
