---
phase: 02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kir/compiler/documents/compiler.py
  - tests/compiler/documents/test_document_compiler.py
autonomous: true
requirements: [STOR-01, STOR-02]
must_haves:
  truths:
    - "After DocumentCompiler.compile(path) returns, the repository contains exactly one artifact keyed by the document's slugified id"
    - "The persisted artifact is a plain-dict representation of the full Document IR (all fields: id, title, source, checksum, language, sections, concepts, glossary, entities, references, diagnostics)"
    - "A test using InMemoryFakeRepository directly asserts that save() was called with the correct artifact_id and that the saved dict contains the expected top-level keys"
    - "Compiling two different Markdown files produces two separate repository artifacts under distinct artifact_id keys — no cross-contamination in the store"
    - "uv run pytest -x -q passes with zero failures after the change"
  artifacts:
    - path: "src/kir/compiler/documents/compiler.py"
      provides: "DocumentCompiler.compile() with repository persistence wired in"
      contains: "repository.save"
    - path: "tests/compiler/documents/test_document_compiler.py"
      provides: "Tests asserting persistence happened (STOR-01/STOR-02)"
      contains: "save"
  key_links:
    - from: "src/kir/compiler/documents/compiler.py"
      to: "src/kir/core/ports/repository_port.py"
      via: "self._ctx.repository.save(doc.id, doc.model_dump())"
      pattern: "repository\\.save"
---

<objective>
Wire DocumentCompiler to RepositoryPort — closing the STOR-01/STOR-02 gap between Phase 1 and Phase 2.

Purpose: Phase 1 built RepositoryPort, YamlFileRepository, and InMemoryFakeRepository (all proven via contract tests). Phase 2 built DocumentCompiler.compile() which runs all four passes and returns a Document IR. The compiler never calls repository.save() — so compiled artifacts are never persisted. This plan closes that gap with a one-line change to compiler.py and targeted test assertions to prove persistence is wired.

Output:
- src/kir/compiler/documents/compiler.py — compile() calls self._ctx.repository.save(doc.id, doc.model_dump()) after the pipeline loop
- tests/compiler/documents/test_document_compiler.py — new test asserting save() was called; updated integration test asserting repository contains the compiled document
</objective>

<execution_context>
@/Users/mniedre/.claude/gsd-core/workflows/execute-plan.md
@/Users/mniedre/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STYLE_GUIDE.md
@.planning/phases/02-document-compiler/02-04b-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire repository.save() into DocumentCompiler.compile()</name>
  <files>src/kir/compiler/documents/compiler.py</files>
  <read_first>
    - src/kir/compiler/documents/compiler.py — current compile() implementation; understand the post-pipeline return point
    - src/kir/core/ports/repository_port.py — RepositoryPort.save(artifact_id: str, artifact: object) -> None signature
    - src/kir/core/passes/context.py — confirm self._ctx.repository is the correct access path
    - src/kir/core/domain/models/document.py — confirm Document.model_dump() is the serialization method (Pydantic BaseModel); confirm doc.id is the slugified string from metadata_pass
    - .planning/STYLE_GUIDE.md — no __future__, no TYPE_CHECKING, direct imports only
  </read_first>
  <behavior>
    - After the pipeline loop completes and ir holds the final Document, call self._ctx.repository.save(ir.id, ir.model_dump()) before returning ir
    - The artifact_id passed to save() is ir.id (the slug produced by metadata_pass, already guaranteed to be safe per ^[A-Za-z0-9_-]+$ by YamlFileRepository._validate_artifact_id)
    - The artifact passed to save() is ir.model_dump() — a plain Python dict; ruamel.yaml can serialize it without any special handling
    - No new imports are needed: self._ctx.repository is already a CompilerContext field; Document.model_dump() is available on all Pydantic models
    - The compile() method continues to return ir (the full Document) to callers — save() is a side-effect, not a replacement for the return value
  </behavior>
  <action>
    In compiler.py, after the for-loop over self._pipeline (line 78-81), and before "return ir", add a single statement: self._ctx.repository.save(ir.id, ir.model_dump())

    The compile() method then reads:
    - Read source text from source_path
    - Construct initial Document IR with empty semantic fields
    - Run each pass_fn in self._pipeline, awaiting async ones
    - self._ctx.repository.save(ir.id, ir.model_dump())
    - return ir

    Do not add any try/except around the save() call — RepositoryPort.save() is expected to raise on error (e.g., invalid artifact_id, filesystem error), and those errors should propagate to the caller. The compiler is a batch tool, not a web service; per CLAUDE.md decision hierarchy, correctness over convenience.

    Do not add new imports. Do not modify the method signature or the return type annotation.
  </action>
  <verify>
    <automated>uv run pytest tests/compiler/documents/test_document_compiler.py -x -q</automated>
  </verify>
  <done>
    - compiler.py contains "repository.save" (grep -c "repository.save" src/kir/compiler/documents/compiler.py returns 1)
    - compile() still returns the final Document IR (existing integration tests still pass)
    - uv run pytest tests/compiler/documents/test_document_compiler.py -x -q passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add persistence assertions to the DocumentCompiler test suite</name>
  <files>tests/compiler/documents/test_document_compiler.py</files>
  <read_first>
    - tests/compiler/documents/test_document_compiler.py — existing test structure; _make_compiler_context() helper; InMemoryFakeRepository usage; all three existing tests
    - tests/core/passes/fakes/fake_repository.py — InMemoryFakeRepository._store dict; how to inspect saved artifacts after compile() returns
    - src/kir/core/domain/models/document.py — Document.model_dump() key names to assert in the saved artifact dict
    - .planning/STYLE_GUIDE.md — no __future__ import, direct imports, f-string assertions
  </read_first>
  <behavior>
    - New test test_compile_persists_document_to_repository: builds a compiler with InMemoryFakeRepository captured as a variable, calls compile(doc_path), then asserts the repository's internal _store contains exactly one entry keyed by the document's id (the slugified H1 title), and that the saved dict has keys "id", "title", "source", "checksum", "language", "sections", "concepts", "glossary", "entities", "references", "diagnostics"
    - New test test_two_documents_produce_two_distinct_repository_entries: compiles doc_a and doc_b using separate compilers each backed by their own InMemoryFakeRepository, asserts each repo's _store has exactly one entry with distinct artifact_id keys, and that neither entry contains the other document's title in its "title" field
    - Do not modify the three existing tests — they must continue to pass unchanged
    - The test file already has "from __future__ import annotations" (pre-existing; do not add it again, do not remove it — removing it is out of scope for this plan)
  </behavior>
  <action>
    Add two new async test functions after the existing three tests:

    test_compile_persists_document_to_repository:
    - Capture the InMemoryFakeRepository by breaking _make_compiler_context() into parts: construct InMemoryFakeRepository(), pass it into CompilerContext manually (mirroring the _make_compiler_context() helper pattern), build DocumentCompiler, call compile(doc_path)
    - Assert len(repo._store) == 1
    - Capture the single stored value: stored = list(repo._store.values())[0]
    - Assert isinstance(stored, dict)
    - Assert set(stored.keys()) >= {"id", "title", "source", "checksum", "language", "sections"}
    - Assert stored["id"] is a non-empty string
    - Assert stored["title"] is a non-empty string
    - Assert the key in repo._store equals stored["id"] (artifact_id == doc.id)

    test_two_documents_produce_two_distinct_repository_entries:
    - Use the doc_a_text and doc_b_text strings from the existing test_no_cross_contamination as source content (or similar alpha/beta content)
    - Build two separate InMemoryFakeRepository instances; build two separate DocumentCompiler instances (mirroring test_no_cross_contamination's separate compiler pattern) each backed by its own repo
    - compile(doc_a_path) and compile(doc_b_path)
    - Assert len(repo_a._store) == 1 and len(repo_b._store) == 1
    - Assert list(repo_a._store.keys())[0] != list(repo_b._store.keys())[0]
    - Assert list(repo_a._store.values())[0]["title"] != list(repo_b._store.values())[0]["title"]

    Import InMemoryFakeRepository is already present in the test file — no new top-level import needed.
    Use the existing fake_output pattern (DocumentExtractionOutput) from the file when constructing contexts, matching the pattern from test_no_cross_contamination.
  </action>
  <verify>
    <automated>uv run pytest tests/compiler/documents/test_document_compiler.py -x -q -v</automated>
  </verify>
  <done>
    - test_document_compiler.py contains "def test_compile_persists_document_to_repository" and "def test_two_documents_produce_two_distinct_repository_entries"
    - uv run pytest tests/compiler/documents/test_document_compiler.py -x -q shows 5 passed (3 existing + 2 new)
    - uv run pytest -x -q passes with 123 total tests (121 existing + 2 new)
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `DocumentCompiler.compile()` | `src/kir/compiler/documents/compiler.py` | Modified method | Adds `self._ctx.repository.save(ir.id, ir.model_dump())` call after pipeline loop |
| `test_compile_persists_document_to_repository` | `tests/compiler/documents/test_document_compiler.py` | New test | Asserts compile() saves one artifact with correct keys to the repository |
| `test_two_documents_produce_two_distinct_repository_entries` | `tests/compiler/documents/test_document_compiler.py` | New test | Asserts two compilations produce two distinct, non-overlapping repository entries |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → DocumentCompiler.compile(source_path) | source_path is caller-provided; no path-traversal restriction at this layer (documented in existing compiler.py docstring — batch tool, not web service) |
| DocumentCompiler → RepositoryPort.save(artifact_id) | artifact_id is doc.id (slugified by metadata_pass); safe pattern ^[A-Za-z0-9_-]+$ enforced by YamlFileRepository._validate_artifact_id at the adapter layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | artifact_id passed to save() | accept | doc.id is produced deterministically by _slugify() in metadata_pass (re.sub on title); YamlFileRepository rejects unsafe values via ValueError; no new trust surface introduced |
| T-02.1-02 | Information Disclosure | Document.model_dump() includes full source text | accept | source text is already in the Document IR which callers receive; saving it to the repo is the explicit purpose of STOR-01; no new exposure |
| T-02.1-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package installs; this plan modifies existing files only |
</threat_model>

<verification>
## Phase 02.1 Verification

```bash
# 1. Repository persistence wired in compiler
grep -c "repository.save" src/kir/compiler/documents/compiler.py
# Expected: 1

# 2. New tests exist
grep -c "test_compile_persists_document_to_repository\|test_two_documents_produce_two_distinct_repository_entries" tests/compiler/documents/test_document_compiler.py
# Expected: 2

# 3. Full test suite green
uv run pytest -x -q
# Expected: 123 passed (or more if baseline changed), 0 failed

# 4. Document compiler tests specifically
uv run pytest tests/compiler/documents/test_document_compiler.py -v
# Expected: 5 tests pass

# 5. No __future__ or TYPE_CHECKING introduced in compiler.py
grep -c "from __future__\|TYPE_CHECKING" src/kir/compiler/documents/compiler.py
# Expected: 0

# 6. Import boundary: compiler.py must NOT import from kir.llm
grep -c "from kir.llm\|import kir.llm" src/kir/compiler/documents/compiler.py
# Expected: 0
```
</verification>

<success_criteria>
- `DocumentCompiler.compile(path)` calls `self._ctx.repository.save(doc.id, doc.model_dump())` exactly once per compilation (one artifact per document — STOR-01)
- The saved artifact is stored under the document's `id` key (the slugified H1 title), not a path or filename that bleeds into the raw source directory (STOR-02 — guaranteed by YamlFileRepository's directory disjointness, which is already tested)
- Five tests in `test_document_compiler.py` all pass, including the two new persistence assertions
- Full test suite `uv run pytest -x -q` passes with 0 failures
- `compiler.py` contains no new imports from `kir.llm`, `kir.tooling`, or any concrete adapter — only the RepositoryPort side-effect via `self._ctx.repository` (the domain-owned port already in CompilerContext)
</success_criteria>

<output>
Create `.planning/phases/02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor/02.1-01-SUMMARY.md` when done.
</output>
