---
phase: 02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor
fixed_at: 2026-07-01T00:00:00Z
review_path: .planning/phases/02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor/02.1-REVIEW.md
iteration: 1
findings_in_scope: 6
fixed: 6
skipped: 0
status: all_fixed
---

# Phase 02.1: Code Review Fix Report

**Fixed at:** 2026-07-01
**Source review:** .planning/phases/02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor/02.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 6
- Fixed: 6
- Skipped: 0

## Fixed Issues

### CR-01: No guard against persisting an empty-id artifact; `save()` exceptions propagate unhandled

**Files modified:** `src/kir/compiler/documents/compiler.py`
**Commit:** 8616f6a
**Applied fix:** Added a pre-save guard that raises `RuntimeError` if `Document.id` is empty after the pipeline loop completes, and wrapped `repository.save()` in a try/except that converts bare exceptions into a structured `RuntimeError` with the document id in the message. This allows callers to distinguish persistence failures from pipeline failures without catching concrete implementation types.

---

### WR-01: `from __future__ import annotations` violates explicit project ban

**Files modified:** `tests/compiler/documents/test_document_compiler.py`
**Commit:** 6106a7f
**Applied fix:** Deleted the `from __future__ import annotations` line (was line 12). The test file has no forward references; all annotations are concrete types with direct imports already in place.

---

### WR-02: Dead compiler construction — `_make_compiler_context()` result is immediately discarded

**Files modified:** `tests/compiler/documents/test_document_compiler.py`
**Commit:** 3c35eb3
**Applied fix:** Deleted the dead `_, _ = _make_compiler_context()  # build compiler via helper then inspect` line from `test_document_compiler_pipeline_has_four_passes`. The remaining direct construction is sufficient for the assertion and the misleading comment is gone.

---

### WR-03: STOR-02 test uses separate repositories — structurally cannot detect same-repository id collision

**Files modified:** `tests/compiler/documents/test_document_compiler.py`
**Commit:** 386199f
**Applied fix:** Rewrote `test_two_documents_produce_two_distinct_repository_entries` to compile both documents into a single shared `InMemoryFakeRepository`. The assertion now checks `len(repo._store) == 2` before asserting `key_a != key_b`, so a bug that always writes to the same key (e.g. `"untitled"`) will fail with a count of 1.

---

### IN-01: `import pytest` is unused

**Files modified:** `tests/compiler/documents/test_document_compiler.py`
**Commit:** dd8830f
**Applied fix:** Removed the `import pytest` line. The test file uses no `pytest.*` symbols; ruff would flag this as F401.

---

### IN-02: Inline imports inside test function bodies

**Files modified:** `tests/compiler/documents/test_document_compiler.py`
**Commit:** 59a5da1
**Applied fix:** Added `from kir.llm.pydantic_ai_adapter import DocumentExtractionOutput, ExtractedConceptDTO` to the module-level import block and removed the four inline occurrences from inside `test_no_cross_contamination`, `test_document_compiler_pipeline_has_four_passes`, `test_compile_persists_document_to_repository`, and `test_two_documents_produce_two_distinct_repository_entries`.

---

_Fixed: 2026-07-01_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
