---
phase: 02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor
plan: "01"
subsystem: compiler
tags: [document-compiler, repository-port, persistence, pydantic, storage]

# Dependency graph
requires:
  - phase: 01-compiler-foundation
    provides: RepositoryPort, YamlFileRepository, InMemoryFakeRepository (contract-tested)
  - phase: 02-document-compiler
    provides: DocumentCompiler.compile() returning fully-populated Document IR
provides:
  - DocumentCompiler.compile() persists compiled Document IR to RepositoryPort on every call
  - test_compile_persists_document_to_repository — asserts one artifact saved with correct keys
  - test_two_documents_produce_two_distinct_repository_entries — asserts no cross-contamination in store
affects: [03-knowledge-compiler, 05-incremental-compilation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Call repository.save(ir.id, ir.model_dump()) after pipeline loop in compile() — one artifact per document"
    - "InMemoryFakeRepository._store used as an inspection point in integration tests"

key-files:
  created: []
  modified:
    - src/kir/compiler/documents/compiler.py
    - tests/compiler/documents/test_document_compiler.py

key-decisions:
  - "repository.save() call is not wrapped in try/except — RepositoryPort.save() is expected to raise on error per correctness-first decision hierarchy"
  - "artifact_id passed to save() is ir.id (slug produced by metadata_pass) — safe pattern enforced by YamlFileRepository._validate_artifact_id at adapter layer"
  - "artifact passed to save() is ir.model_dump() — plain Python dict; ruamel.yaml can serialize without special handling"

patterns-established:
  - "Persistence as compiler side-effect: compile() returns ir AND saves to repository, callers do not need to call save() separately"

requirements-completed: [STOR-01, STOR-02]

# Metrics
duration: 5min
completed: 2026-07-01
---

# Phase 02.1 Plan 01: Wire DocumentCompiler to RepositoryPort Summary

**DocumentCompiler.compile() now persists Document IR to RepositoryPort via ir.model_dump() after the pipeline loop, closing the STOR-01/STOR-02 gap with a one-line change and two targeted test assertions**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-07-01T09:30:55Z
- **Completed:** 2026-07-01T09:35:10Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `self._ctx.repository.save(ir.id, ir.model_dump())` to `DocumentCompiler.compile()` after the pipeline loop — one artifact persisted per compilation, keyed by the document's slugified id
- Added `test_compile_persists_document_to_repository` — asserts `len(repo._store) == 1`, correct dict keys, and that the artifact_id key matches `stored["id"]`
- Added `test_two_documents_produce_two_distinct_repository_entries` — asserts two separate compilers with separate repos each contain exactly one entry with distinct keys and titles

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire repository.save() into DocumentCompiler.compile()** - `53c766f` (feat)
2. **Task 2: Add persistence assertions to the DocumentCompiler test suite** - `0b8f60f` (test)

**Plan metadata:** (see final docs commit)

_Note: Both tasks had tdd="true". RED state was confirmed manually for Task 1 (repository._store was empty before save() was added). Tests for Task 2 went GREEN immediately since Task 1's implementation was already in place._

## Files Created/Modified

- `src/kir/compiler/documents/compiler.py` — Added single `self._ctx.repository.save(ir.id, ir.model_dump())` line after pipeline loop, before `return ir`
- `tests/compiler/documents/test_document_compiler.py` — Added two new async test functions: `test_compile_persists_document_to_repository` and `test_two_documents_produce_two_distinct_repository_entries` (122 lines added)

## Decisions Made

- No try/except around `repository.save()` — per CLAUDE.md decision hierarchy (Correctness > Developer Convenience), errors from RepositoryPort should propagate to callers unchanged; the compiler is a batch tool, not a web service
- artifact passed as `ir.model_dump()` (plain dict) — ruamel.yaml can serialize without special handling; no schema migration needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- STOR-01 and STOR-02 are now closed — DocumentCompiler persists artifacts to RepositoryPort
- All 123 tests pass (121 baseline + 2 new)
- Phase 3 (Knowledge Compiler) can build on DocumentCompiler knowing artifacts land in the repository after compilation
- YamlFileRepository (already tested via contract tests) is the production-ready repository adapter for file-backed artifact storage

---
*Phase: 02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor*
*Completed: 2026-07-01*

## Self-Check: PASSED

- FOUND: src/kir/compiler/documents/compiler.py
- FOUND: tests/compiler/documents/test_document_compiler.py
- FOUND: .planning/phases/02.1-close-gap-stor-01-stor-02-wire-documentcompiler-to-repositor/02.1-01-SUMMARY.md
- FOUND: 53c766f feat(02.1-01): wire repository.save() into DocumentCompiler.compile()
- FOUND: 0b8f60f test(02.1-01): add persistence assertions for STOR-01/STOR-02
